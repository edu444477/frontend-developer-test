/** @type {import('tailwindcss').Config} */
export default {
  // Files not listed here can use Tailwind classes, but no CSS will be
  // generated for them.
  content: ['./index.html', './src/**/*.{js,jsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
}
